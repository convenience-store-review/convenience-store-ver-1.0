<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<context:component-scan base-package="org.zerock.user.controller" />
	
    <context:component-scan base-package="org.zerock.user.persistence" />
    <context:component-scan base-package="org.zerock.user.service" />
	
	
	<!-- 인터셉터 샘플 -->
	<beans:bean id="sampleInterceptor" class="org.zerock.user.interceptor.SampleInterceptor" />
	   <interceptors>
       <interceptor>
			<mapping path="/user/doA" />
			<mapping path="/user/doB" />
			<beans:ref bean="sampleInterceptor" />
       </interceptor>
   </interceptors>
	
    <!-- 로그인 처리 -->
    <beans:bean id="loginInterceptor" class="org.zerock.user.interceptor.LoginInterceptor" />
    <!-- 접근 권한 처리 -->
    <beans:bean id="authInterceptor" class="org.zerock.user.interceptor.AuthInterceptor" />
    
	<interceptors>
		<interceptor>
           <mapping path="/user/loginPost"/>
           <beans:ref bean="loginInterceptor" />
		</interceptor>
		
		<interceptor>
           <mapping path="/sboard/list"/>
           <mapping path="/sboard/register"/>
           <mapping path="/sboard/modifyPage"/>
           <mapping path="/sboard/removePage"/>
           <beans:ref bean="authInterceptor" />
		</interceptor>
	</interceptors>
	
	
</beans:beans>
